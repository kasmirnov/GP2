# Анализ рынка IT вакансий

Анализ IT вакансий сбором данных с HeadHunter и Хабр.Карьера с помощью Web-Scraping и Api , дальнейший анализ средствами pandas, numpy, matplotlib, plotly.

## 🎯 Структура репозитория

``````
.
├─ configs/
│  └─ config.json                # dictConfig для логов и базовые настройки
├─ data/
│  ├─ raw/                       # сырые выгрузки (.jsonl)
│  ├─ clean/                     # финальные таблицы CSV
├─ lib/
│  ├─ mylogger.py                # JSON-formatter
├─ logs/
│  └─ logs.log                   # лог-файл
├─ notebooks/
│  └─ hh_habr.ipynb              # анализ датасетов
├─ scripts/
│  ├─ hh_api.py                  # api HH → data/raw/hh.jsonl
│  ├─ hh_make_csv.py             # пасинг HH → data/clean/hh.csv
│  ├─ habr_scraping.py           # web-scraping Habr → data/raw/habr.jsonl
│  └─ habr_make_csv.py           # парсинг Habr → data/clean/habr.csv
├─ README.md
├─ requirements.txt
└─ .gitignore
``````
# Логирование

Настройка логирования происходит за счёт настройки config.json, в том числе вкл/выкл логирования, устанавливая соответсвующее значение enabled: true/false.
Логи идут в файл logs/logs.log с возможностью настройки максимального размера одного файла логов и количества backup'ов через config

# Стек

- Python 3.10+
- requests
- selectolax
- logging
- pandas, numpy, matplotlib, plotly
- python-dateutil

# Установка и запуск

Необходимо склонировать репозиторий, установить библиотеки
```
python -m venv .venv
. .venv/Scripts/activate      # Windows
# source .venv/bin/activate   # Linux/Mac

pip install -r requirements.txt
```
Для получения итоговых csv файлов нужно последовательно запустить скрипты:
```
# HeadHunter
python -m scripts.hh_api
python -m scripts.hh_make_csv

# Habr Career
python -m scripts.habr_scraping
python -m scripts.habr_make_csv
```
Итоговые файлы будут находиться в /data/clean/

# Описание столбцов итогового датасета:


| Поле                   | Тип              | Описание / Пример                                           |
| ---------------------- | ---------------- | ----------------------------------------------------------- | 
| `id`                   | `int`            | Идентификатор вакансии                                      | 
| `title`                | `str`            | Заголовок вакансии                                          | 
| `description`          | `str`            | Текст описания                                              | 
| `currency`             | `str`            | Валюта зарплаты: `RUR`, `USD`, …                            | 
| `employer`             | `str`            | Работодатель                                                | 
| `city`                 | `str`            | Город                                                       | 
| `night_shifts`         | `bool`           | Ночные смены: `True/False`                                  | 
| `internship`           | `bool`           | Стажировка: `True/False`                                    | 
| `address_city`         | `str`            | Город из блока `address`                                    | 
| `address_lat`          | `float`          | Широта `lat`                                                | 
| `address_lng`          | `float`          | Долгота `lng`                                               | 
| `schedule_id`          | `str`            | Код графика работы (напр. `fullDay`, `shift`)               | 
| `schedule_name`        | `str`            | Название графика работы                                     |
| `work_format_id`       | `str`            | Код формата работы (офис/удалёнка/гибрид)                   | 
| `work_format_name`     | `str`            | Название формата работы                                     | 
| `working_hours_code`   | `str`            | Код часов работы                                            |
| `working_hours_name`   | `str`            | Название часов работы                                       | 
| `experience_code`      | `str`            | Код опыта работы (`noExperience`, `between1And3`, …)        | 
| `experience_name`      | `str`            | Нзвание уровня опыта работы                                 | 
| `employment`           | `str`            | Тип занятости (`full`, `part`, `project`)                   | 
| `remote`               | `bool`           | Удалённая работа/нет                                        | 
| `published_at`         | `str` (ISO-8601) | Время публикации, ISO-строка (`YYYY-MM-DDTHH:MM:SS±ZZZZ`)   | 
| `url`                  | `str`            | Прямая ссылка на вакансию                                   | 
| `salary_value`         | `float`          | Числовая зарплата                                           | 
| `levels`               | `list[str]`      | Уровни/категории (напр. `["Junior","Middle"]`)              | 
| `skills`               | `list[str]`      | Навыки                                                      |
| `company_name`         | `str`            | Имя компании                                                |
| `working_requirements` | `list[str]`      | Плашки условий/требований (например, «Можно удалённо»)      |
| `vacancy_name`         | `str`            | Заголовок вакансии на Habr                                  |


Основные результаты анализа
•Зарплаты

Средняя зарплата в IT выше рынка: медиана 120–150 тыс ₽, топ-10 % — 250–300 тыс ₽ и выше.

Разрыв между грейдами:

Junior — 60–100 тыс ₽

Middle — 120–180 тыс ₽

Senior — 200–350 тыс ₽

Лидеры по зарплатам: Москва, Санкт-Петербург, но Казань, Екатеринбург и Новосибирск приближаются к ним.

•Востребованные навыки

ТОП-навыки: Python, SQL, Linux, Git, Docker, JavaScript, React, Kubernetes, Django, Pandas.

Самые «денежные» навыки: Apache Airflow, LLM, Английский язык, DevOps.

•Опыт и грейды

Востребованы все уровни — от стажёров до senior-специалистов.

Middle-специалисты — самая стабильная и ценная категория на рынке.

Чем выше опыт, тем чаще встречается удалённый формат работы.

•Компании-лидеры

Больше всего вакансий размещают: Яндекс, VK, Сбер, Тинькофф, Ozon, Лаборатория Касперского.

Топ-работодатели в среднем платят на 20–40 % выше рынка, чаще действуют в финтехе, e-commerce и data-инфраструктуре.

•Вакансии для студентов

10–12 % рынка занимают предложения для стажёров и без опыта.

Частые роли: Junior-разработчик, аналитик, тестировщик, support-инженер.

Зарплаты — в 2–3 раза ниже рынка (40–70 тыс ₽), но сегмент служит кадровым резервом.

•География и формат работы

Основные центры: Москва, Санкт-Петербург, Екатеринбург, Новосибирск, Казань, Краснодар.

Удалёнка — ≈30 % всех IT-вакансий, гибрид — в основном у крупных компаний.

Преобладает фултайм, но растёт доля частичной и проектной занятости.

•Ключевые выводы

IT остаётся самой высокодоходной отраслью на российском рынке труда.

Спрос на специалистов всех уровней остаётся стабильным и растущим.

Data Science, DevOps, Backend и Cloud-решения — самые перспективные направления.

Компании, предлагающие удалённый формат и обучение, получают преимущество в найме.

Для удержания кадров важны прозрачные грейды, бонусы и быстрая коммуникация.





















